# Release Evidence

This directory is the repository-local landing zone for volatile release-candidate and published-verification evidence such as GitHub Actions run ids, artifact ids, artifact digests, tarball shasums, downloaded artifact paths, registry summaries, and command transcripts.

Evidence files here are intentionally outside the npm package boundary. The published package may include stable release process docs, but it must not include volatile target-SHA evidence that changes whenever a fresh release-candidate workflow runs.

For a fresh alpha release-candidate review:

1. Confirm `git rev-parse HEAD`, `git rev-parse origin/main`, and the remote `origin/main` SHA all match.
2. Trigger `.github/workflows/release-candidate.yml` for that commit.
3. Download all five artifacts and normalize them into one local review directory:
   ```bash
   npm run release:artifacts:normalize -- --download-dir <gh-download-dir> --out-dir <normalized-artifact-dir>
   ```
   The normalizer emits `schemaVersion: "agent-cli-runtime.releaseArtifactNormalization.v1"`, copies only `npm-pack.json`, `package-files.txt`, `gate-evidence.json`, `release-verification.json`, and `agent-cli-runtime-*.tgz`, and rejects missing, duplicate, or unknown files without printing absolute local paths. The `agent-cli-runtime-gate-evidence` artifact must include `daemon:verify`, `runtime:safety`, and `compat:real:evidence:verify`. Remote clean-checkout workflow artifacts record repo-only real compatibility evidence as `repo_only_not_run` / `not_refreshed_in_ci`; local strict release evidence is required when the release conclusion depends on the P8-2 matrix matching the target SHA.
4. Run `npm run release:verify -- --dir <normalized-artifact-dir>`.
5. Record the run URL, head SHA, artifact list, verification result, local gate results, and go/no-go decision in a local evidence file under this directory. Main-scoped P8 evidence is generated with `npm run release:main-candidate:evidence -- --stage <stage> --release-target-sha <origin-main-sha> --local-release-dir <local-strict-dir> --remote-run-json <run.json> --artifacts-json <artifacts.json> --downloaded-dir <normalized-artifact-dir> --out .release-evidence/<stage-lower>-main-release-candidate.json`.

If the commit under review has not reached `main` / `origin/main`, record the evidence explicitly as branch evidence. Use the branch ref in `gh workflow run release-candidate.yml --ref <branch>`, confirm the workflow `headSha` equals the branch target SHA, and keep `mainEvidence: false` plus the observed `originMainShaAtTrigger` in the evidence file. Do not describe branch evidence as current `main` release evidence.

P6-4 uses `.release-evidence/p6-4-remote-release-candidate.json` for this branch-evidence shape. It records the remote run id, target branch/SHA, artifact ids/digests, downloaded `release:verify` result, and the P6-3 compatibility verifier gate summary. It must not include raw workflow logs, raw CLI output, full prompts, token values, Bearer values, auth env assignment values, private absolute paths, or local temp download paths.

P6-5 uses `.release-evidence/p6-5-main-release-candidate.json` for main-scoped evidence after P6-1 through P6-4 are merged to `main`. It records the `main` target SHA, merge PR, workflow run id/URL, artifact names/ids/digests, downloaded `release:verify` result, and the release gate evidence summary. It must not include raw workflow logs, raw CLI output, full prompts, token values, Bearer values, auth env assignment values, private absolute paths, or local temp download paths.

P6-6 uses `.release-evidence/p6-6-main-head-release-candidate.json` for the fresh `main` release-candidate evidence after P6-5 itself is merged to `main`. It records the new `main` target SHA, workflow run/job summary, artifact names/ids/digests, downloaded `release:verify` result, and the release gate evidence summary. It must not include raw workflow logs, raw CLI output, full prompts, token values, Bearer values, auth env assignment values, private absolute paths, local temp download paths, tarball size, tarball shasum, or pack shasum.

P7-2 uses `.release-evidence/p7-2-alpha-2-main-release-candidate.json` for the fresh `main` release-candidate evidence after P7-1 is merged to `main`. It records the alpha.2 target SHA, package version, workflow run/job summary, artifact names/ids/digests, downloaded `release:verify` result, and release gate evidence summary. It must not include raw workflow logs, raw stdout/stderr, raw CLI output, full prompts, token values, Bearer values, auth env assignment values, private absolute paths, local temp download paths, tarball size, tarball shasum, or pack shasum. It is not npm publish evidence and does not create a GitHub Release, npm token, or trusted publishing configuration.

P7-3 uses `.release-evidence/p7-3-alpha-2-publish.json` for alpha.2 publish dry-run evidence. It records the fresh `main` release-candidate workflow, artifact names/ids/digests, downloaded `release:verify` result, registry pre-publish state, local dry-run verification summary, and the explicit dry-run-only publish mode. It must not include raw workflow logs, raw stdout/stderr, raw CLI output, full prompts, token values, Bearer values, auth env assignment values, private absolute paths, local temp download paths, tarball size, tarball shasum, pack shasum, or package integrity values. Without separate maintainer authorization it records `npmPublishExecuted: false` and `githubRelease.created: false`.

P7-4 uses `.release-evidence/p7-4-alpha-2-final-publish-lock.json` for alpha.2 final pre-publish lock evidence after the packaged docs publish-state drift is fixed and merged to `main`. It records the fresh `main` release-candidate workflow whose `headSha` equals `origin/main`, artifact names/ids/digests, downloaded `release:verify` result, registry pre-publish state, local dry-run publish result, and `publishMode: "final-lock-only"`. It must not include raw workflow logs, raw stdout/stderr, raw CLI output, full prompts, token values, Bearer values, auth env assignment values, private absolute paths, local temp download paths, tarball size, tarball shasum, package integrity values, or pack shasum. Without same-message explicit authorization for real publishing it records `npmPublishExecuted: false` and `githubRelease.created: false`.

If P7-4 receives same-message explicit authorization for real publishing but npm rejects the publish request before creating `agent-cli-runtime@0.1.0-alpha.2`, record the blocked attempt in `.release-evidence/p7-4-alpha-2-real-publish-attempt-blocked.json`. It records the target SHA, pre-publish E404 check, redacted npm auth/permission failure class, unchanged registry state, and `githubRelease.created: false`. It must not include npm debug logs, raw stdout/stderr, raw CLI output, full prompts, token values, Bearer values, auth env assignment values, private absolute paths, local temp paths, tarball size, tarball shasum, package integrity values, or pack shasum.

P7-4 post-publish evidence uses `.release-evidence/p7-4-alpha-2-post-publish.json`. It records the fresh `main` release-candidate workflow for the publish target, downloaded `release:verify` result, npm E404 precheck, successful `npm publish --ignore-scripts --tag alpha`, post-publish registry version/dist-tag checks, temporary installed-package CLI smoke, GitHub pre-release verification, and published-state docs update. It must not include npm debug logs, raw stdout/stderr, raw CLI output, full prompts, token values, Bearer values, auth env assignment values, private absolute paths, local temp paths, tarball size, tarball shasum, package integrity values, or pack shasum.

P7-5 uses `.release-evidence/p7-5-alpha-3-corrective-release.json` for the alpha.3 stale package-docs corrective release. It records the alpha.2 stale-docs diagnosis, branch/base SHA state, local packaged-docs tarball inspection, dry-run publish state, registry precheck, and the authorization boundary for real publish/deprecate/GitHub Release actions. After the P7-5 docs/version changes are merged to `main`, fresh main release-candidate workflow evidence must be added with `headSha` equal to `origin/main`, all five artifacts downloaded, and `npm run release:verify -- --dir <normalized-downloaded-artifact-dir>` passing. If real publish is authorized, the same evidence file records post-publish registry, installed-package smoke, registry tarball package-docs inspection, optional alpha.2 deprecation state, and GitHub pre-release verification. It must not include npm debug logs, raw stdout/stderr, raw CLI output, full prompts, token values, Bearer values, auth env assignment values, private absolute paths, local temp paths, tarball size, tarball shasum, package integrity values, pack shasum, run logs, or local unpack paths.

P7-6 uses `.release-evidence/p7-6-alpha-3-main-release-candidate.json` for alpha.3 main-scoped release-candidate evidence after P7-5 is merged to `main`. It records the target main SHA, P7-5 merge PR, workflow run/job summary, artifact names/ids/digests, downloaded `release:verify` result, registry precheck, local main release gates, release gate evidence summary, and release action summary. P7-6 post-publish evidence uses `.release-evidence/p7-6-alpha-3-post-publish.json`; it records authorized npm publish, registry version/dist-tags, GitHub pre-release verification, published package gates, registry packaged-docs inspection, and post-publish verifier result. These files must not include npm debug logs, raw stdout/stderr, raw CLI output, full prompts, token values, Bearer values, auth env assignment values, private absolute paths, local temp paths, tarball size, tarball shasum, package integrity values, pack shasum, run logs, or local unpack paths.

For a fresh published package verification review, trigger `.github/workflows/published-package-verification.yml`, confirm the run `headSha`, download `agent-cli-runtime-published-verification`, run `npm run published:verify:evidence -- --dir <normalized-downloaded-artifact-dir>`, and record only redacted summary metadata here.

P8-1 uses `.release-evidence/p8-1-published-usability.json` for clean external consumer usability evidence. It records `npm install agent-cli-runtime@<version>`, package-root ESM import, installed `agent-runtime agents --json`, installed `agent-runtime doctor --json`, fake CLI conformance, fake CLI run, and fake CLI goal results as redacted command summaries. It must not include the temporary consumer path, raw stdout/stderr, full prompts, token values, Bearer values, auth env assignment values, private absolute paths, downloaded artifacts, or local `node_modules` paths.

P8-2 uses `.release-evidence/p8-2-real-cli-compatibility-matrix.json` for real Codex / Claude Code / OpenCode compatibility matrix evidence. It records `schemaVersion: "agent-cli-runtime.realCompatibilityMatrix.v1"`, `checkedAt`, `packageVersion`, `gitSha`, `gitDirty`, `gitInputDirty`, `gitOutputDirty`, `dirtySummary`, and per-adapter executable resolution, version, auth, model source, capabilities, argv profile, parser mode, prompt transport, safe preflight, optional smoke, diagnostics, and `needsVerification`. `gitDirty` / `gitInputDirty` describe dirty non-evidence inputs; `gitOutputDirty` describes the matrix output file itself. It must not include raw stdout/stderr, full prompts, token values, Bearer values, auth env assignment values, private absolute paths, local temp paths, raw observed text, CLI debug logs, or resolved executable paths. Resolved executable paths must be represented as `<resolved_executable>`, and unavailable adapters must include an explicit reason.

For a real CLI compatibility refresh, run `npm run compat:real:evidence` for safe preflight only. Add authenticated smoke evidence only with explicit `--allow-real-run --agent <id> --expect-text <text>` pairs. The evidence file should keep summarized classifications, versions, auth/model sources, `needsVerification` decisions, cwd-mutation result fields, and explicit input/output dirty summary fields only; do not store raw CLI stdout/stderr or full prompts.

After writing real compatibility evidence, run `npm run compat:real:evidence:verify`. The verifier is offline: it reads `.release-evidence/p8-2-real-cli-compatibility-matrix.json` by default, supports `--file <path>` for alternate or legacy P6 evidence, and does not start authenticated real agent runs. It rejects unsafe content, missing dirty-state evidence, skipped/auth-missing/unavailable states claimed as success, incomplete authenticated success evidence, missing adapter fields, missing required `needsVerification` audit items, and invalid repo-only package-boundary claims. Use `npm run compat:real:evidence:verify -- --self-test` to exercise the verifier's local rejection fixtures.

For release review, run `npm run compat:real:evidence:verify -- --target-sha <target-sha> --max-age-hours 24 --release-strict`. This checks that the evidence target SHA matches the release target SHA, the evidence is fresh, and dirty non-evidence inputs are rejected unless `--allow-dirty` is explicitly supplied. Dirty changes limited to the matrix output file are reported as `dirtyPolicy.status: "self_dirty_only"` and are valid for release-strict review. A default verifier run can still accept dirty repo-only evidence, but it reports `dirtyPolicy.status` and must not be used as clean release evidence.

`prepublish:check` runs the default offline verifier. Local `release:candidate` defaults to `--real-compatibility-mode local-strict` and runs the target-SHA/freshness-bound verifier against the existing repo-only compatibility matrix. The default local-strict target is the matrix `gitSha`, not the evidence-recording commit HEAD; pass `--target-sha <sha>` only for an explicit override. The command does not run `npm run compat:real:evidence`, does not pass `--allow-real-run`, and does not refresh authenticated real CLI evidence. Remote clean-checkout workflows pass `--real-compatibility-mode repo-only-skipped` and record that repo-only real compatibility evidence was not refreshed in CI. Release-candidate `gate-evidence.json` stores only the compatibility gate command, ok flag, verifier schema, verified evidence schema, target SHA status, freshness status, dirty policy status, diagnostic count/codes, and fixed repo-only skip reason when applicable. Do not copy the raw `.release-evidence/p8-2-real-cli-compatibility-matrix.json` file, raw stdout/stderr, diagnostic messages, full prompts, private paths, tokens, Bearer values, or auth env values into release artifacts.

P8-4 uses `.release-evidence/p8-4-release-strict-compatibility.json` for release-strict compatibility closure. Generate it after the target commit exists and after local strict release-candidate artifacts have been created:

```bash
npm run release:strict-compatibility:evidence -- --target-sha <target-sha> --local-release-dir <tmp-local-strict>
```

The summary records `stage`, `targetSha`, `targetShaSource`, `checkedAt`, matrix/verifier schemas, strict compatibility verifier result, local strict release-candidate verification, remote workflow trigger state, downloaded-artifact verification state, `noAuthenticatedRealRun`, `noNpmPublish`, `noNpmToken`, and branch/main evidence scope. `targetSha` is the release target proven by the matrix; `currentHeadSha` is the summary generation context and can differ after a repo-only evidence commit. If the target SHA is not in `origin/main`, record only branch/local evidence with `branchEvidence: true`, `mainEvidence: false`, `remoteReleaseCandidate.triggered: false`, and `downloadedArtifacts.verified: false`. Do not trigger a main-scoped workflow, and do not describe the result as current `main` release evidence. If the target SHA is in `origin/main`, fresh main evidence still requires a new `.github/workflows/release-candidate.yml --ref main` run whose `headSha` equals the target SHA, all five artifacts are present, and `npm run release:verify -- --dir <normalized-downloaded-artifact-dir>` passes.

P8-5 uses `.release-evidence/p8-5-main-release-candidate.json` for main-scoped remote release-candidate closure. It records `releaseTargetSha`, `mainEvidence: true`, `branchEvidence: false`, matrix target SHA, strict verifier summary, local strict release-candidate verification, fresh remote run metadata, artifact names/count, downloaded artifact verification, and remote gate summary. The generator entrypoint is `npm run release:main-candidate:evidence`. It requires `releaseTargetSha == origin/main == workflow headSha`; the remote run must be `workflow_dispatch`, `headBranch: main`, `status: completed`, and `conclusion: success`. The artifact metadata must contain exactly `agent-cli-runtime-tarball`, `agent-cli-runtime-pack-metadata`, `agent-cli-runtime-package-files`, `agent-cli-runtime-gate-evidence`, and `agent-cli-runtime-release-verification`, with one explicitly non-expired artifact per name and a `sha256:` digest for each. Downloaded artifacts are normalized with `npm run release:artifacts:normalize -- --download-dir <gh-download-dir> --out-dir <normalized-downloaded-artifact-dir>`; each expected file must be under its matching downloaded artifact directory before `npm run release:verify -- --dir <normalized-downloaded-artifact-dir>`. P8-5 is not npm publish evidence and must not record raw workflow logs, raw stdout/stderr, full prompts, token values, Bearer values, auth env assignment values, private absolute paths, local temp download paths, resolved executable paths, tarball shasums, or pack shasums.

P8-7 uses `.release-evidence/p8-7-main-release-candidate.json` for the fresh main release-candidate closure after P8-6 release evidence tooling hardening is on `main`. It is generated with `npm run release:main-candidate:evidence -- --stage P8-7 --release-target-sha <origin-main-sha> --local-release-dir <local-strict-dir> --remote-run-json <run.json> --artifacts-json <artifacts.json> --downloaded-dir <normalized-artifact-dir> --out .release-evidence/p8-7-main-release-candidate.json`. It records `releaseTargetSha`, `originMainShaAtCheck`, workflow `headSha`, run id/url, exact five artifact names/count/digests/`expired: false`, local strict release verification, P8-6 normalized downloaded artifact verification, target-SHA-only evidence scope, `noAuthenticatedRealRun`, `noNpmPublish`, and `noNpmToken`. It requires `releaseTargetSha == origin/main == workflow headSha`; the remote run must be `workflow_dispatch`, `headBranch: main`, `status: completed`, and `conclusion: success`. The evidence-recording commit and any later PR merge commit require fresh main evidence before being treated as release targets. It is not npm publish evidence and must not record raw workflow logs, raw stdout/stderr, full prompts, token values, Bearer values, auth env assignment values, private absolute paths, local temp download paths, resolved executable paths, tarball shasums, or pack shasums.

P8-8 uses `.release-evidence/p8-8-package-content-equivalence.json` for repo-only package content drift review between a recorded release target SHA and a later ref. Generate it with `npm run release:package-content:verify -- --base-ref <release-target-sha> --head-ref <sha-or-ref> --out .release-evidence/p8-8-package-content-equivalence.json`. The verifier emits `schemaVersion: "agent-cli-runtime.packageContentEquivalence.v1"` and compares npm package file lists plus per-file content hashes from temporary git worktrees. It does not use gzip/tarball bytes as the only equivalence signal. If package-visible files differ, record `packageContentEqual: false` and `freshReleaseCandidateRequired: true`. If changes are limited to package-external evidence, tests, or repo-only scripts, record `packageContentEqual: true`, `evidenceOnlyDrift: true`, and `freshReleaseCandidateRequired: false`. P8-8 does not turn a later HEAD into P8-7 workflow evidence; it only determines whether package content is equal. It must not include raw stdout/stderr, workflow logs, full prompts, token values, Bearer values, auth env assignment values, private absolute paths, local temp paths, worktree paths, tarball shasums, or pack shasums.

Do not put `.reference/`, temporary download directories, private user paths, CI tokens, npm tokens, or real provider tokens in this directory.
